【测试方式】

1.      打开到 Lab1/test 文件夹
2.      用命令 “chmod +x test.sh”来添加脚本
3.      用命令 “./test.sh”开始测试
3.5     使用配置文件可以定制自己需要的测试
4.      现在你可以去追剧、学习、运动、睡觉了have a nice day!
-------------------------------------
【注意事项】

1.      确保c++版本可以支持c++11
2.      确保你的shell是bash,查看方式使用命令
        “cat /etc/shells | grep bash”发现有输出“/bin/bash”
3.      在测试过程种不要强制终止程序，如果中止可以用
        “chmod +x reset.sh”和“./reset.sh”来重置环境可以放
        心使用，只会删除你一个人的log文件，不会删别人的。
4.      没啥事别断电，因为断电影响性能，如果你是台式当我没
        说。如果可以的话，你可以在测试老师的程序时适当断电，
        这样我们的程序就会显得更加高效。
5.      在正确性测试之前一定要用
        "cat ./filenames|../src/Sudoku/sudoku -i > test2.result"
        然后测试正确性，才能在diff.diff中看到结果.但是其实
        不用看，因为我们已经用5个、14个文件测试过了。
------------------------------------
【配置文件】
1.      文件名：config.ini
2.      有两个节testing,func。分别有注释
3.      func里面的_f结尾表示那个测试的标志位，标志位为1即
        测试，标志位为0 不测试第一次运行无脑全部为1。
        pre_f是编译
        create_file_f是生成测试文件
        init_f是初始化文件
        acc_test_f是正确性测试中生成标准答案，